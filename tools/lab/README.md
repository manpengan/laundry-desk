# 实机 / 现场证据实验室（Grok）

**证据强度 ≥ 结论强度**：没有实机就写「待实测」，禁止把 mock 标成通过。

| 目录                           | 用途                                        |
| ------------------------------ | ------------------------------------------- |
| [windows-edge/](windows-edge/) | Windows 冷启动 / WSS / LNA / 防火墙演练清单 |
| [printers/](printers/)         | 三台打印机实机样张与回传模板                |

跑完后把去 EXIF 的证据放在各自 `evidence/`（默认 gitignore 大媒体；小文本可入库）。
