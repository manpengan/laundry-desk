{
  "name": "@laundry/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "bootstrap:local": "pnpm --filter @laundry/contracts build && pnpm --filter @laundry/domain build && pnpm run build && node dist/local/bootstrap-cli.js",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "lint": "eslint . --ext .ts,.tsx --max-warnings=0",
    "test": "tsc -p tsconfig.json && node --test --test-concurrency=1 'dist/**/*.test.js'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@fastify/cors": "^11.3.0",
    "@laundry/contracts": "file:../../packages/contracts",
    "@laundry/domain": "file:../../packages/domain",
    "@node-rs/argon2": "^2.0.2",
    "fastify": "^5.10.0",
    "pg": "^8.22.0",
    "sharp": "^0.35.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/pg": "^8.20.0"
  }
}
