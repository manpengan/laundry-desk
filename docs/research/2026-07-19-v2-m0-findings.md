# laundry-v2 M0 技术验证阶段发现报告 (findings)

日期：2026-07-19  
本报告记录了 v2 里程碑 M0 阶段的技术难点攻坚实测结论与证据。

---

## M0-1: RLS 三元租户隔离 + 性能

_主责：Codex_

_(由 Codex 分支测试结论填入)_

---

## M0-2: Primary lease 时序 + 可信时间

_主责：Codex_

_(由 Codex 分支测试结论填入)_

---

## M0-3: 三类打印机实机

_主责：Grok_

_(由 Grok 分支测试结论填入)_

---

## M0-4: Edge 本地通道 + 冷启动 + A/B 升级

_主责：Grok_

_(由 Grok 分支测试结论填入)_

---

## M0-5: 三 adapter × 模型工具调用兼容矩阵

_主责：Gemini_

### 结论：【待实测】

我们设计并验证了统一的消息信封，成功抹平了 `anthropic` (官方 SDK)、`openai-compat` (通用 OpenAI 格式，支持 DeepSeek/DashScope 环境变量路由) 和 `gemini` (使用最新的原生 `@google/genai` 2.12.0 SDK) 的调用细节差异。

> **说明**：当前演练逻辑跑在安全 Mock 模式上（输出带 `[MOCK_MODE]` 横幅），仓库零落盘密钥。待由 manpengan 提供真实 API Key 环境变量并完成真实模型连续 3 次稳定性实测后，结论将正式更新为【通过】。

### 六维方言与接口差异比对矩阵

| 比对维度 | Anthropic (Claude) | OpenAI / Compat (GPT/DeepSeek/Qwen) | Gemini (GoogleGenAI SDK) |
| :--- | :--- | :--- | :--- |
| **1. System 消息** | 必须作为 API 顶层 `system` 属性传入，不可混入 messages 数组 | 混入 messages 数组中，作为 `role: 'system'` 传入 | config 属性下的 `systemInstruction` 字段传入 |
| **2. Assistant 角色** | `'assistant'` | `'assistant'` | `'model'` |
| **3. 多 Tool 并行支持** | 允许，单回复 content 中返回多个 `{ type: 'tool_use' }` | 允许，Choice Message 中返回包含多元素的 `tool_calls` 数组 | 允许，候选 Parts 数组中包含多个 `functionCall` 元素 |
| **4. 工具定义格式** | 顶层 `name`, `description`, `input_schema` | 包装于 `type: 'function'` 之下的 parameters 规范 | Native 结构，定义在 `functionDeclarations` 中 |
| **5. 流式 Tool Delta** | 监听 `content_block_delta`，流式累加 `input_json_delta` | 监听 `delta.tool_calls` 各索引 chunk 累加 `arguments` 字符串 | 原生 SDK 的 `generateContentStream` 处理，Delta 级触发 `functionCall` |
| **6. 结束条件与 Token 用量** | 结束原因 `response.stop_reason`；用量 `response.usage` (`input_tokens`/`output_tokens`) | 结束原因 `choice.finish_reason`；用量 `response.usage` (`prompt_tokens`/`completion_tokens`) | 结束原因 `candidate.finishReason`；用量 `response.usageMetadata` (`promptTokenCount`/`candidatesTokenCount`) |

### 验证证据 [MOCK_MODE]
验证脚本在未设置线上 key 环境变量时，会自动切入透明 Mock 模式，输出带有明确 `[MOCK_MODE]` 横幅标识的完整仿真数据。
本地执行复现命令：
```bash
npx tsx tools/spikes/m0-5-adapters/run.ts
```
已实现连续 3 次稳定性闭环演练（单工具与并行双工具，非流式与流式），完整原样控制台输出日志请参见 [m0-5-evidence.log](../../tools/spikes/m0-5-adapters/evidence/m0-5-evidence.log)。

---

## M0-6: 本地单机模式跑通

_主责：Gemini_

### 结论：【通过】

本地单机 Compose 运行完满通过。我们成功设计了基于 Postgres 16 镜像的开发环境，并集成了 Mock Edge 本地微服务网关 (端口 8080) 与 Mock Cloud 中央 Agent 控制台 (端口 8081) 分立占位容器，用于完整演练“假开单 -> 打印(mock) -> 正常取衣”的 HTTP 与 DB 往返流。

### 多租户 RLS 与四元外键架构实测证据 [REAL_MODE]
在本地 Docker 容器网络中进行真实数据读写与安全拦截演练。
本地执行复现命令：
```bash
./tools/spikes/m0-6-compose/smoke-test.sh
```
控制台真实执行完整日志证据（已同步记录在 [m0-6-evidence.log](../../tools/spikes/m0-6-compose/evidence/m0-6-evidence.log)）：

```text
=== [M0-6 Spike] [LOCAL_ONLY] 1. Rebuilding and launching docker-compose services ===
✔ Postgres container is HEALTHY.
✔ Mock Edge Server container is HEALTHY.
✔ Mock Cloud Server container is HEALTHY.

=== [M0-6 Spike] 3. Starting HTTP API Round-Trip & Security Boundary Tests ===

[Step A] Creating order (Order: ord_1001, Tenant: org_aaa/store_1)...
{
   "message" : "Order created successfully under RLS context",
   "success" : true
}
✔ [PASS] Step A (Create order) succeeded.

[Step B] Fetching & Printing Receipt (Order: ord_1001, Tenant: org_aaa/store_1)...
{
   "receipt" : "\n========================================\n       MOCK LAUNDRY RECEIPT (EDGE)\n========================================\nTenant Org  : org_aaa\nStore ID    : store_1\nOrder ID    : ord_1001\nCustomer    : 张三\nDate        : 2026-07-20T01:21:19.349Z\n----------------------------------------\nOrder Lines :\n - Line ID: line_1, Price: ¥29.00\n----------------------------------------\nGarment Items :\n - Barcode: BC_0001, Status: received\n========================================\n",
   "success" : true
}
✔ [PASS] Step B (Print receipt) succeeded.

[Step C - SECURITY] Attempting unauthorized pickup using Tenant B context...
✔ [PASS] Security Isolation Working! Server blocked unauthorized request with status 404 (Not Found or RLS violation).

[Step D] Performing legitimate pickup using Tenant A context...
{
   "success" : true,
   "updated_garment" : {
      "barcode" : "BC_0001",
      "id" : "garm_1",
      "order_id" : "ord_1001",
      "order_line_id" : "line_1",
      "org_id" : "org_aaa",
      "status" : "picked_up",
      "store_id" : "store_1"
   }
}
✔ [PASS] Step D (Legitimate pickup) succeeded.

=== [M0-6 Spike] 4. Running Database Constraint and internal RLS Validation ===
Running Test 1: default-closed behavior without session variables...
 count 
-------
     0
(1 row)

Running Test 2: reading data inside legitimate session...
BEGIN
SET
SET
    id    | customer_name 
----------+---------------
 ord_1001 | 张三
(1 row)

COMMIT
Running Test 3: checking multi-tenant isolation under tenant org_bbb...
BEGIN
SET
SET
 count 
-------
     0
(1 row)

COMMIT
Running Test 4: checking cross-order foreign key constraint breach prevention...
BEGIN
SET
SET
INSERT 0 1
INSERT 0 1
COMMIT

[Test 4 Constraint Action] Attempting to insert a garment with mismatched order_id and order_line_id...
ERROR:  insert or update on table "garments" violates foreign key constraint "garments_org_id_store_id_order_id_order_line_id_fkey"
DETAIL:  Key (org_id, store_id, order_id, order_line_id)=(org_aaa, store_1, ord_2002, line_1) is not present in table "order_lines".
✔ [PASS] 四元外键真实生效：跨订单挂靠被 FK 拦截。

=== [M0-6 Spike] All Smoke Tests Completed successfully ===
```
